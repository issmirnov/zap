build:
  main: .
  binary: zap
  goos:
    - darwin
    - linux
archive:
  name_template: '{{ .ProjectName }}_{{ .Os }}_{{ .Arch }}'
  replacements:
    amd64: 64-bit
    386: 32-bit
    darwin: MacOS
    linux: Linux
  files:
    - c.yml
    - README.md
    - LICENSE
